const first = 5;
const second = 4;
if(first > second) {
    console.log('first is bigger')
  }
  else if(first < second) {
    console.log('first is not bigger')
  }
  else {console.log('first is equal second')}